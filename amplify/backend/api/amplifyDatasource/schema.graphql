type TaskModel @model @auth(rules: [{allow: public}]){
  id: ID!
  title: String!
  body: String
  dateCreated: AWSDateTime
  state: StateEnum
}

enum StateEnum {
  NEW
  ASSIGNED
  IN_PROGRESS
  COMPLETE
}
